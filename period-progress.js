(()=>{
const MODES=['cards','quiz','person','chrono'];
const aliases=window.JUZDEREK_TOPIC_ALIASES||{'Ежелгі Парсы мемлекеті':'ancient-persia','Ежелгі Грекия өркениеті':'ancient-greece'};
function safe(){try{return JSON.parse(localStorage.getItem('juzderek_topics_progress')||'{}')}catch{return{}}}
function info(id){const t=safe()[id]||{},scores=t.scores||{};const done=MODES.filter(m=>scores[m]&&Number(scores[m].pct)===100);const pct=Math.round(done.length/MODES.length*100);const next=MODES.find(m=>!done.includes(m))||'cards';const played=MODES.map(m=>({mode:m,at:scores[m]?.finishedAt||0})).filter(x=>x.at).sort((a,b)=>b.at-a.at);return{done,pct,next,last:played[0]?.mode||null,updatedAt:t.updatedAt||played[0]?.at||0}}
function resolve(card){const text=(card.textContent||'').trim();const name=Object.keys(aliases).find(n=>text.includes(n));return name?{id:aliases[name],name}:null}
function decorate(){document.querySelectorAll('.topic-card').forEach(card=>{const meta=resolve(card);if(!meta)return;const p=info(meta.id);card.classList.add('topic-progress-card');card.classList.toggle('topic-complete',p.pct===100);card.classList.toggle('topic-started',p.pct>0&&p.pct<100);let box=card.querySelector('.topic-live-progress');if(!box){box=document.createElement('div');box.className='topic-live-progress';card.appendChild(box)}const icon=p.pct===100?'✓':'◔';const label=p.pct===100?'Аяқталды':p.pct>0?'Орындалуда':'Басталмаған';const html=`<div class="tlp-lite"><div class="tlp-status"><span class="tlp-state-icon">${icon}</span><span>${label}</span></div><strong class="tlp-percent">${p.pct}%</strong></div><div class="tlp-track"><i style="width:${p.pct}%"></i></div>`;if(box.innerHTML!==html)box.innerHTML=html;});}
let timer=0;function schedule(){clearTimeout(timer);timer=setTimeout(decorate,20)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',schedule);else schedule();window.addEventListener('storage',schedule);window.addEventListener('pageshow',schedule);new MutationObserver(schedule).observe(document.body,{childList:true,subtree:true});
})();