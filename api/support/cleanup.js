const {supabase,sendError}=require('./_lib');
module.exports=async(req,res)=>{try{const expected=process.env.CRON_SECRET;if(!expected||String(req.headers.authorization||'')!==`Bearer ${expected}`)return res.status(401).json({error:'Unauthorized'});const cutoff=new Date(Date.now()-30*86400000).toISOString();await supabase(`support_conversations?last_sender=eq.admin&last_message_at=lt.${encodeURIComponent(cutoff)}`,{method:'DELETE',headers:{Prefer:'return=minimal'}});return res.status(200).json({ok:true,cutoff})}catch(err){sendError(res,err)}};