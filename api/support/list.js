const {clean,validStudentId,supabase,sendError}=require('./_lib');
module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const studentId=clean(req.query?.studentId,80);if(!validStudentId(studentId))return res.status(400).json({error:'Қате student ID'});const items=await supabase(`support_tickets?student_id=eq.${encodeURIComponent(studentId)}&select=id,kind,message,reply,status,created_at,answered_at&order=created_at.desc&limit=20`);res.setHeader('Cache-Control','no-store');return res.status(200).json({items:Array.isArray(items)?items:[]})}catch(err){sendError(res,err)}};