const {clean,validStudentId,supabase,sendError}=require('./_lib');
module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const studentId=clean(req.query?.studentId,80);if(!validStudentId(studentId))return res.status(400).json({error:'Қате student ID'});const conversations=await supabase(`support_conversations?student_id=eq.${encodeURIComponent(studentId)}&select=id&limit=1`);const conversation=Array.isArray(conversations)?conversations[0]:null;if(!conversation){res.setHeader('Cache-Control','no-store');return res.status(200).json({items:[]})}const items=await supabase(`support_messages?conversation_id=eq.${encodeURIComponent(conversation.id)}&select=id,sender,kind,body,page,created_at&order=created_at.asc&limit=300`);res.setHeader('Cache-Control','no-store');return res.status(200).json({conversationId:conversation.id,items:Array.isArray(items)?items:[]})}catch(err){sendError(res,err)}};